# 和风天气 API 配置
# 获取地址: https://dev.qweather.com/
# 控制台: https://console.qweather.com/

# 方式一：API Key 认证（简单，但安全性较低）
# 注意：从2027年2月1日起将逐步限制每日请求数量
VITE_QWEATHER_API_KEY=your_api_key_here

# 方式二：JWT 认证（推荐，更安全）
# 1. 在控制台创建凭据，选择 JWT 认证
# 2. 使用以下命令生成密钥对（已包含在项目中）：
#    openssl genpkey -algorithm ED25519 -out ed25519-private.pem
#    openssl pkey -pubout -in ed25519-private.pem -out ed25519-public.pem
# 3. 将 ed25519-public.pem 中的公钥添加到控制台
VITE_QWEATHER_API_ID=your_api_id_here
QWEATHER_PRIVATE_KEY_PATH=./ed25519-private.pem
